# roadmap.kurult.ai

Canonical Parse + Hulegu launch roadmaps for [Kurultai](https://kurult.ai). Rewritten 2026-08-20 by Ghazan (Now/Next/Later). x402 OFF. No certifications. No monthly dollars. Scoring weights unpublished. Ship OFF. No secrets. Host URL not posted.

## Invariants

- No build system, no framework, no webfonts, no analytics. The committed files are the artifact.
- Ghazan owns product truth. Live Parse claims only from parsethis.ai llms.txt, /v1/pricing, /docs, and product-truth-card.
- No books, no personal mail, no secrets, API tokens, agent UUIDs, or `.env` in this repo.
- Nobody sends. Do not dump vault .env tokens.
- R-3 and R-5 killed: no minted Access key; do not share one in PRODUCT.
- One roadmap repo only: `Danservfinn/roadmap.kurult.ai`. Do not invent a second repo.
- Host URL not posted. Orda 020: unauth is the Access wall, not 522. Do not invent DNS. Do not touch `kurult.ai` MX / mail records.
- Chrome is Batu light full-width paper — not a 920px memo column. No Palatino, no ember rail, no starfield.

## Host

Host URL not posted. Do not invent DNS. Do not mint or write an Access key, login URL, token, or `.env`.

R-3 and R-5 stay Killed. This PR stays draft.

## Source

Static files in this repo are the board copy. They are not served by Cloudflare Pages auto-deploy, `wrangler pages deploy`, or a Pages custom-domain attach. Those paths are not the host story for this site.

## Verify after host is pointed (Kublai)

```sh
dig +short kurult.ai MX                        # unchanged — mail records are never touched
```

Do not treat a Pages URL or a `*.pages.dev` hostname as this origin. Do not post a live roadmap URL until Kublai has pointed the hostname and Orda has receipted.
